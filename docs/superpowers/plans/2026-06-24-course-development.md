# Course Development System Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build the course-development system (scaffolding + reusable dev workflow script) and validate it by developing module M2 (quantization pipeline) through the full 5-phase workflow.

**Architecture:** Notebook-only course (each Step = one self-contained `.ipynb` with fill-in blanks + ipytest L1 + tiny-model L2 + H200 L3 cells). A reusable Workflow script (`workflows/dev-module.js`) orchestrates the 5 phases per module: developer → architect review → QA loop → student worktree loop → finalize. Scaffolding is hand-built; notebook content is generated by the workflow's developer agent and validated by its QA/student loops.

**Tech Stack:** uv (env management), Python 3.11, pytest/ipytest/nbval/nbconvert (testing), llmcompressor + vLLM (course subject), H200×8, the Workflow tool (orchestration), git worktrees (student isolation).

**Spec:** [`docs/superpowers/specs/2026-06-24-course-development-design.md`](../specs/2026-06-24-course-development-design.md)

## Global Constraints

- 两条硬约束：**C1** 每 Step 配 UT（ipytest cell）；**C2** 逻辑能跑通、框架级问题开发期就抓（L2 tiny 真模型 cell 当场跑）。
- 测试工具在 `envs/quant`：`ipytest`、`nbval`、`nbconvert`/`jupyter`。
- notebook-only：无 `.py` lesson、无 jupytext；每 Step 一个 `.ipynb`，自包含。
- 每 Step ≥2 个填空（学员实现 `logic` 函数），每个填空有 ipytest 测试；填空设计由开发团队决策。
- H200 cell 用 `if torch.cuda.is_available():` 守卫；提交前 `nbconvert --clear-output`。
- 已查证版本基线（见 OUTLINE 技术栈表）：vLLM 0.x、llmcompressor 0.12.x（要 transformers v5）、compressed-tensors 0.17.x、CUDA 12.8/driver 570、Python 3.11。
- 在 `course-development` 分支上开发（已含基础文件）。

## File Structure

- `envs/quant/pyproject.toml` — 加测试工具依赖（ipytest/nbval/nbconvert/jupyter）。
- `course/NOTEBOOK_CONVENTIONS.md` — notebook cell 布局约定（开发专家/评审/student 共享）。
- `course/m2-quant-pipeline/README.md` — M2 导读 + Setup 前置（env/model/校准数据）。
- `course/m2-quant-pipeline/steps/` — M2 的 3 个 notebook（由 workflow 生成，非本计划手写）。
- `workflows/dev-module.js` — 可复用开发 workflow（5 阶段，按模块参数化）。
- `docs/superpowers/plans/2026-06-24-course-development.md` — 本文件。

---

### Task 1: 给 envs/quant 加测试工具依赖

**Files:**
- Modify: `envs/quant/pyproject.toml`（dependencies 数组）
- Test: 手动 `uv run --directory envs/quant python -c "import ipytest; import nbval"` 通过

**Interfaces:** Produces 一个装好 ipytest/nbval/nbconvert 的 quant env，供后续 notebook 执行/测试。

- [ ] **Step 1: 编辑 `envs/quant/pyproject.toml`，在 `dependencies` 数组加入 4 项**

在 `[project]` 的 `dependencies = [ ... ]` 里追加（保持其它项不变）：

```toml
    "ipytest>=0.14",           # notebook 内跑 pytest（隔离测试填空）
    "nbval>=0.11",             # pytest --nbval 执行 notebook 做输出回归
    "nbconvert>=7.0",          # nbconvert --execute / --clear-output
    "jupyter>=1.0",            # 学员交互跑 notebook
```

- [ ] **Step 2: 重新 lock + sync**

Run: `uv lock --directory envs/quant && uv sync --directory envs/quant`
Expected: 解析成功，生成/更新 `envs/quant/uv.lock`，装入 `.venv`。

- [ ] **Step 3: 验证导入**

Run: `uv run --directory envs/quant python -c "import ipytest, nbval, nbconvert; print('ok')"`
Expected: 打印 `ok`，无 ImportError。

- [ ] **Step 4: 提交**

```bash
git add envs/quant/pyproject.toml envs/quant/uv.lock
git commit -m "build(quant): add ipytest/nbval/nbconvert/jupyter for notebook testing"
```

---

### Task 2: 建 course/ 骨架 + M2 README/Setup

**Files:**
- Create: `course/NOTEBOOK_CONVENTIONS.md`（本任务先建空骨架占位，Task 3 填内容）—— 实际 Task 3 单独建，本任务不建。
- Create: `course/m2-quant-pipeline/README.md`
- Create: `course/m2-quant-pipeline/steps/.gitkeep`
- Create: `course/m1-activation-outliers/.gitkeep`、`course/m3-tuning-eval/.gitkeep`、`course/m4-deploy-loop/.gitkeep`（占位，后续模块用）

**Interfaces:** 建立 `course/<module>/` 目录约定（README + steps/），workflow 的开发者 agent 往 `steps/` 写 notebook。

- [ ] **Step 1: 核对模型基线一致**

核对 `scripts/download_model.sh` 的 `MODEL_ID` 应为 `Qwen/Qwen2.5-7B-Instruct`（spec 统一基线，与下面 README 引用一致），`LOCAL_DIR` 默认路径一致；不符则改为 Instruct（base 无 chat_template，影响评测口径）。

- [ ] **Step 2: 建目录结构**

Run:
```bash
mkdir -p course/m2-quant-pipeline/steps \
         course/m1-activation-outliers course/m3-tuning-eval course/m4-deploy-loop
touch course/m2-quant-pipeline/steps/.gitkeep \
      course/m1-activation-outliers/.gitkeep \
      course/m3-tuning-eval/.gitkeep \
      course/m4-deploy-loop/.gitkeep
```

- [ ] **Step 3: 写 `course/m2-quant-pipeline/README.md`**（完整内容）

```markdown
# M2 — H200×8 端到端量化流水线（FP8 / AWQ / SmoothQuant）

对应 [OUTLINE.md](../../../OUTLINE.md) 模块 2。本课用 llm-compressor 把 Qwen2.5-7B-Instruct 量化为三种格式，产出可被 vLLM 加载的模型。

## Setup 前置（开始 steps 前完成）

1. **环境**（一次性，两个隔离 uv env）：
   ```bash
   bash scripts/setup_env.sh
   ```
   产出 `envs/quant/.venv`（量化用）与 `envs/deploy/.venv`（部署用）。本模块全程用 `envs/quant`。
2. **拉基线模型**（H200×8 单节点下到共享路径，无需每卡各下一份）：
   ```bash
   HF_TOKEN=xxxx bash scripts/download_model.sh /shared/models/Qwen2.5-7B-Instruct
   ```
3. **校准数据**（AWQ / SmoothQuant 需要；FP8 不需要）：
   在 quant env 里跑（每个需要校准的 notebook 首部都有这 cell）：
   ```python
   from datasets import load_dataset
   from transformers import AutoTokenizer
   tok = AutoTokenizer.from_pretrained("/shared/models/Qwen2.5-7B-Instruct")
   ds = load_dataset("wikitext", "wikitext-2-raw-v1", split="train").shuffle(seed=42).select(range(512))
   calib = [tok(d["text"], return_tensors="pt").input_ids[0][:2048] for d in ds if d["text"].strip()]
   ```

## Steps（每个都是一个自包含 notebook，含填空 + ipytest + tiny 验证 + H200 执行）

- `steps/s1_fp8_quant.ipynb` — FP8 W8A8（`QuantizationModifier(scheme="FP8_DYNAMIC")`，无需校准，H200 首选）
- `steps/s2_awq_quant.ipynb` — AWQ W4A16（llm-compressor 统一路径；附 AutoAWQ 遗留对照）
- `steps/s3_smoothquant.ipynb` — SmoothQuant W8A8（两段式：SmoothQuant + GPTQ），末尾含三方法产物对比 finale

跑法：`uv run --directory envs/quant jupyter lab` 打开 notebook；按序填空 → 跑 ipytest 测试 cell → 跑 tiny 验证 cell → 跑 H200 执行 cell。
```

- [ ] **Step 4: 提交**

```bash
git add course/ scripts/download_model.sh
git commit -m "feat(course): scaffold course/ + M2 README/Setup; align download_model.sh to Instruct baseline"
```

---

### Task 3: 写 notebook 约定文档（开发/评审/student 共享）

**Files:**
- Create: `course/NOTEBOOK_CONVENTIONS.md`

**Interfaces:** workflow 的开发者/评审/student agent 都读这份文档，保证 notebook 布局一致。

- [ ] **Step 1: 写 `course/NOTEBOOK_CONVENTIONS.md`**（完整内容）

````markdown
# Notebook 约定（所有 Step 通用）

每个 Step = 一个自包含 `.ipynb`，cell 顺序与类型固定如下：

1. **标题 markdown cell**：`# Step N: <名称>` + 一句话目标 + 对应 OUTLINE 课时。
2. **导入 cell**：`import`（含 `%%capture` 抑制冗余输出）。
3. **讲解 markdown cells**：原理 / 公式 / 这步做什么（分几段）。
4. **填空代码 cells**：每个 `logic` 函数一个 cell，形如：
   ```python
   def build_fp8_recipe(ignore=("lm_head",)):
       # TODO: 返回 QuantizationModifier(targets="Linear", scheme="FP8_DYNAMIC", ignore=list(ignore))
       raise NotImplementedError
   ```
   每 Step ≥2 个填空，逐步搭出本步能力。
5. **ipytest 测试 cell（L1）**：
   ```python
   %%ipytest -qq
   def test_build_fp8_recipe_ignores_lm_head():
       r = build_fp8_recipe()
       assert "lm_head" in r.ignore
       assert r.scheme == "FP8_DYNAMIC"
   ```
   学员填完函数跑此 cell 验证；隔离执行，避免状态串扰。
6. **tiny 模型验证 cell（L2，CPU）**：内存 tiny Qwen2 + 真跑 llmcompressor + assert：
   ```python
   from transformers import Qwen2Config, Qwen2ForCausalLM
   tiny = Qwen2ForCausalLM(Qwen2Config(num_hidden_layers=2, hidden_size=64,
       intermediate_size=128, num_attention_heads=2, num_key_value_heads=2, vocab_size=320))
   # 用学员的函数跑真 llmcompressor（tiny 规模），assert 产物含 quantization_config
   ```
   FP8 等必须 FP8 硬件的：这 cell 标注"CPU 跑不了，见 L3"。
7. **H200 执行 cell（L3，GPU 守卫）**：
   ```python
   import torch
   if torch.cuda.is_available():
       run_fp8_quantize("/shared/models/Qwen2.5-7B-Instruct", "./out/qwen-fp8")
   else:
       print("跳过：无 GPU（CPU 环境只跑 L1/L2）")
   ```
8. **产物检查 cell**（折入原 2.6）：打印 `config.json` 的 `quantization_config` + compressed-tensors 结构 + 显存对比。

**提交规则**：`jupyter nbconvert --clear-output --inplace steps/sN_*.ipynb` 后再 commit（不带输出）。
**填空设计**：由开发专家 + 架构师定（用户全权委托）；硬性 ≥2 个有意义填空、每个有 ipytest 测试。
````

- [ ] **Step 2: 提交**

```bash
git add course/NOTEBOOK_CONVENTIONS.md
git commit -m "docs(course): notebook cell conventions for dev/review/student"
```

---

### Task 4: 写可复用开发 workflow `workflows/dev-module.js`

**Files:**
- Create: `workflows/dev-module.js`

**Interfaces:** 通过 `Workflow({scriptPath: "workflows/dev-module.js", args: {module: "m2-quant-pipeline"}})` 调用；按模块参数化。产出该模块的 notebook + 各轮评审结果。

- [ ] **Step 1: 写 `workflows/dev-module.js`**（完整内容）

```javascript
export const meta = {
  name: 'dev-module',
  description: '课程模块开发 workflow：开发专家→架构师审核→QA 评审 loop→student worktree 试课 loop→定稿',
  phases: [
    { title: '开发', detail: '开发专家写模块全部 notebook（填空+ipytest+tiny+H200 cell）' },
    { title: '架构审核', detail: '架构师审一致性/填空/执行/连贯' },
    { title: 'QA 评审', detail: '评审员严格审，循环到 pass（封顶 3 轮）' },
    { title: 'Student 试课', detail: 'worktree 真跑 H200 + 评课程设计，循环到满意（封顶 3 轮）' },
    { title: '定稿', detail: '输出验收结果（merge/commit 由调用方做）' },
  ],
}

const MODULE = args.module                 // 例 "m2-quant-pipeline"
const MODULE_PATH = `course/${MODULE}`
const MAX_QA_ROUNDS = 3
const MAX_STUDENT_ROUNDS = 3
const CONV = 'course/NOTEBOOK_CONVENTIONS.md'   // 约定文档，agent 读取
const SPEC = 'docs/superpowers/specs/2026-06-24-course-development-design.md'

const DEV_REPORT = {
  type: 'object', additionalProperties: false,
  properties: {
    notebooks: { type: 'array', items: { type: 'string' } },          // 写了哪些 .ipynb 路径
    blanksTotal: { type: 'number' },                                   // 总填空数（每步≥2）
    execResults: { type: 'array', items: { type: 'object', additionalProperties: false,
      properties: { notebook:{type:'string'}, l1:{type:'string'}, l2:{type:'string'}, l3:{type:'string'} },
      required: ['notebook','l1','l2','l3'] } },                       // 各 cell 执行结果 pass/skip/fail
    notes: { type: 'string' },
  },
  required: ['notebooks','blanksTotal','execResults','notes'],
}

const REVIEW = {
  type: 'object', additionalProperties: false,
  properties: {
    verdict: { type: 'string', enum: ['pass', 'needs_revision'] },
    findings: { type: 'array', items: { type: 'object', additionalProperties: false,
      properties: { severity:{type:'string',enum:['critical','major','minor']}, location:{type:'string'}, issue:{type:'string'}, action:{type:'string'} },
      required: ['severity','location','issue','action'] } },
    summary: { type: 'string' },
  },
  required: ['verdict','findings','summary'],
}

const STUDENT_FB = {
  type: 'object', additionalProperties: false,
  properties: {
    codeRun: { type: 'object', additionalProperties: false,
      properties: { filledBlanksOk:{type:'boolean'}, notebooksRan:{type:'array',items:{type:'string'}}, errors:{type:'array',items:{type:'string'}} },
      required: ['filledBlanksOk','notebooksRan','errors'] },
    designIssues: { type: 'array', items: { type: 'string' } },        // 连贯/清晰/概念缺口/教学法/填空难度
    satisfied: { type: 'boolean' },                                    // 代码跑通 AND 设计满意
    report: { type: 'string' },
  },
  required: ['codeRun','designIssues','satisfied','report'],
}

// ---------- ① 开发 ----------
phase('开发')
const devReport = await agent(`你是【课程开发专家】，开发模块 ${MODULE}（路径 ${MODULE_PATH}）。
先读 ${CONV}（notebook cell 约定）与 ${SPEC}（§4/§5 结构、§11 验收）与 OUTLINE.md 对应模块。
为本模块每个 Step 创建一个 .ipynb（路径 ${MODULE_PATH}/steps/），严格按 ${CONV} 的 cell 顺序：
标题→导入→讲解→填空代码 cell（每步≥2 个 logic 函数 TODO）→ipytest 测试 cell(L1)→tiny 验证 cell(L2)→H200 执行 cell(L3,GPU 守卫)→产物检查 cell。
填空设计由你定（用户全权委托）：选有教学意义的 logic 函数，难度递进，每个填空都有 ipytest 测试。
**写完当场验证跑通**：在 envs/quant 跑 L1（ipytest，可用参考实现填空后测）+ L2（tiny 模型，CPU）+ L3（H200，真 Qwen2.5-0.5B 再 7B，若无 GPU 则记录 skip）。
提交前对每个 notebook 跑 \`jupyter nbconvert --clear-output --inplace <nb>\`。
按 schema 报告：写了哪些 notebook、总填空数、各 cell 执行结果、备注。`, { label: '开发专家', phase: '开发', schema: DEV_REPORT })

// ---------- ② 架构师审核 ----------
phase('架构审核')
const archReview = await agent(`你是【课程架构师】，审核模块 ${MODULE}（${MODULE_PATH}/steps/）。
读 ${CONV} + OUTLINE 对应模块 + 开发报告：${JSON.stringify(devReport)}。
审：cell 布局是否符合约定、Step 衔接、填空设计合理性（教学意义+难度）、notebook 是否真能跑通（L1/L2/L3）、整体连贯。
如需返工，直接在 notebook 里改（你有编辑权），并把 critical/major 改掉。按 schema 输出 verdict/findings/summary。`,
  { label: '课程架构师(审核)', phase: '架构审核', schema: REVIEW })

// ---------- ③ QA 评审 loop ----------
phase('QA 评审')
let qaResult = null
for (let i = 0; i < MAX_QA_ROUNDS; i++) {
  const qa = await agent(`你是【质量保证评审员】，严格审模块 ${MODULE}（${MODULE_PATH}/steps/）。
读 ${CONV} + ${SPEC}(§11 验收) + OUTLINE 对应模块。严格审：准确性/完备/填空+ipytest 覆盖/跑通证据(L1/L2/L3)/教学法+连贯。
findings 用 severity 标注，每条给 action。verdict=pass 仅当无 critical/major。按 schema 输出。`,
    { label: `质量保证评审员(r${i+1})`, phase: 'QA 评审', schema: REVIEW })
  qaResult = qa
  log(`QA 第 ${i+1} 轮 verdict=${qa.verdict}, findings=${qa.findings.length}`)
  if (qa.verdict === 'pass') break
  await agent(`你是【课程架构师】，按 QA 评审修模块 ${MODULE}（${MODULE_PATH}/steps/）。
评审：${JSON.stringify(qa)}。改掉所有 critical/major，合理采纳 minor。改完重跑受影响 notebook 的 L1/L2 cell 验证。`,
    { label: `课程架构师(修·QA r${i+1})`, phase: 'QA 评审' })
}

// ---------- ④ Student 试课 loop ----------
phase('Student 试课')
let studentResult = null
for (let i = 0; i < MAX_STUDENT_ROUNDS; i++) {
  const fb = await agent(`你是【Student/学员】，真实用户代理，试课模块 ${MODULE}。
你会得到一个隔离 git worktree（课程副本）。两端评估：
(代码端) uv sync(envs/quant) → 按 ${MODULE_PATH}/README Setup 拉模型 → 按序打开每个 notebook：自己**填空**(实现 logic 函数) → 跑 ipytest 测试 cell → tiny 验证 cell → H200 执行 cell。记录能否填出、跑通/报错。
(知识端) 通读全章，评知识连贯/讲解清晰/逻辑跳跃/概念缺口/教学法/填空难度，凡觉得设计不好的都记。
按 schema 输出：codeRun(filledBlanksOk/notebooksRan/errors)、designIssues、satisfied(代码跑通 AND 设计满意)、report。`,
    { label: `Student(r${i+1})`, phase: 'Student 试课', schema: STUDENT_FB, isolation: 'worktree' })
  studentResult = fb
  log(`Student 第 ${i+1} 轮 satisfied=${fb.satisfied}`)
  if (fb.satisfied) break
  await agent(`你是【课程架构师+评审员】联合，按 student 反馈修模块 ${MODULE}。
反馈：${JSON.stringify(fb)}。架构师改代码/结构/填空，评审员定夺教学法/连贯问题。改完重跑 L1/L2 验证。`,
    { label: `架构师+评审员(修·student r${i+1})`, phase: 'Student 试课' })
}

// ---------- ⑤ 定稿（输出验收；merge/commit 由调用方做） ----------
phase('定稿')
const acceptance = {
  module: MODULE,
  notebooks: devReport.notebooks,
  blanksPerStepOk: devReport.blanksTotal >= 2 * (devReport.notebooks.length || 1),
  qaPassed: qaResult && qaResult.verdict === 'pass',
  studentSatisfied: studentResult && studentResult.satisfied,
  qaRounds: qaResult ? qaResult.verdict : 'unfinished',
  studentRounds: studentResult ? studentResult.satisfied : 'unfinished',
}
log(`定稿 ${MODULE}: ${JSON.stringify(acceptance)}`)
return { devReport, archReview, qaResult, studentResult, acceptance }
```

- [ ] **Step 2: 静态自检（JS 语法 + meta 纯字面量）**

Run: `node --check workflows/dev-module.js 2>&1 || echo "(Workflow 脚本非纯 Node，语法检查可能报 export/meta；人工核对：meta 纯字面量、无 Date.now/Math.random、await 在顶层异步上下文)"`
Expected: 无明显语法错；人工确认 meta 为纯字面量、无禁用 API。

- [ ] **Step 3: 提交**

```bash
git add workflows/dev-module.js
git commit -m "feat(workflow): reusable dev-module workflow (dev→architect→QA loop→student worktree loop)"
```

---

### Task 5: M2 试水 — 跑 dev-module workflow 并验收

**Files:**
- 由 workflow 生成：`course/m2-quant-pipeline/steps/s1_fp8_quant.ipynb`、`s2_awq_quant.ipynb`、`s3_smoothquant.ipynb`

**Interfaces:** 调用 Task 4 的 workflow，参数 `module: "m2-quant-pipeline"`。验收对齐 spec §11。

- [ ] **Step 1: 跑 workflow**

调用 Workflow（后台运行，完成时通知）：
```
Workflow({ scriptPath: "workflows/dev-module.js", args: { module: "m2-quant-pipeline" } })
```
用 `/workflows` 看实时进度。

- [ ] **Step 2: 取回结果 + 检查 acceptance**

workflow 完成后，读取其返回的 `acceptance` 对象。验收（spec §11，全部要满足）：
- `notebooks` 含 3 个（fp8/awq/smoothquant）。
- `blanksPerStepOk` = true（每步 ≥2 填空）。
- 每个 notebook 含 ipytest 测试 cell(L1) + tiny 验证 cell(L2) + H200 执行 cell(L3) + 产物检查 cell。
- `qaPassed` = true（QA verdict=pass）；`studentSatisfied` = true。
- 抽查：`uv run --directory envs/quant jupyter nbconvert --execute course/m2-quant-pipeline/steps/s1_fp8_quant.ipynb` 在 H200 上整本跑通无错（CPU 环境则 L3 skip、L1/L2 过）。

- [ ] **Step 3: 不通过则迭代**

若 acceptance 未全过：根据 `qaResult.findings` / `studentResult` 直接修 notebook（或重跑 workflow 的对应阶段），再验证。封顶：QA/student 各已封顶 3 轮仍不过 → 升级人工。

- [ ] **Step 4: 提交 M2 产物**

```bash
# 清输出后提交
for nb in course/m2-quant-pipeline/steps/*.ipynb; do
  uv run --directory envs/quant jupyter nbconvert --clear-output --inplace "$nb"
done
git add course/m2-quant-pipeline/
git commit -m "feat(m2): quant pipeline notebooks (FP8/AWQ/SmoothQuant) via dev-module workflow"
```

- [ ] **Step 5: 试水复盘**

确认开发系统（骨架 + workflow + 三层测试）在 M2（最重、框架风险最高）上跑通；记录 H200 真实跑时，据此定 student loop 封顶轮数（暂定 3）；据此微调 workflow 与约定后，再开 M1/M3/M4。

---

## 后续模块（本计划之后）

M2 试水通过后，M1/M3/M4 各自重跑 `workflows/dev-module.js`（`args.module` 改为对应模块），无需新计划：
- `m1-activation-outliers`（原理，代码最轻）
- `m3-tuning-eval`（调优 + 评测，吃 M2 的 SmoothQuant 产物）
- `m4-deploy-loop`（vLLM 部署 + 闭环，吃 M2/M3 产物）
